/**
 * The error code denoting a script tag wasn't loaded.
 * @type {string}
 */
export const code = 'ERROR_NECESSARY_SCRIPT_NOT_LOADED';

/**
 * The error message thrown when a script tag wasn't loaded.
 * @type {string}
 */
export const message = 'Failed to load necessary script tags upon window load';
